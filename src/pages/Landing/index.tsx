import React from 'react';

const Landing = () => {
  return (
    <div
      style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        height: '100vh',
        backgroundColor: 'red',
      }}>
      Landing
    </div>
  );
};

export default Landing;
